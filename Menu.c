int OP;

do
{
    printf("Enter 1 or 2 for the operation: /n");
    scanf("%d", &OP);
} while(OP > 2 || OP < 1)

if (OP == 1)
{
	printf("%d * %d = %d\n" = V1, V2,V1 + V2);
}
else
{
	printf("%d * %d = %d\n" = V1, V2,V1 * V2);
}